package com.tatalance.profile;

/**
 * Types of business profiles an account (owner) can have.
 * Used to scope jobs while sharing clients across profiles.
 */
public enum ProfileType {
    DRIVER,
    ENGINEER,
    HANDYMAN,
    OTHER
}
